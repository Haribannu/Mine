class Solution{
    static int map(int n, String keys[], int arr[], String s)
    {
     Map<String,Integer> m=new HashMap<>();
     for(int i=0;i<keys.length;i++)
     {
     m.put(keys[i],arr[i]);
     }
     return m.get(s)==null? -1:m.get(s);
    }
}
